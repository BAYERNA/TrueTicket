server:
  port: 8081

spring:
  application:
    name: ticket-service
  datasource:
    url: jdbc:postgresql://localhost:5432/ticket_service
    username: ticket
    password: ticket
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration

bot-detection-service:
  url: ${BOT_DETECTION_SERVICE_URL:http://localhost:8091}

security:
  jwt:
    secret: ${JWT_SECRET}
    issuer: ${JWT_ISSUER:trueticket}
    access-token-minutes: ${JWT_ACCESS_TOKEN_MINUTES:60}
  internal-api-key: ${INTERNAL_API_KEY}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

resilience4j:
  circuitbreaker:
    instances:
      bot-detection-service:
        sliding-window-size: 20
        failure-rate-threshold: 50
        wait-duration-in-open-state: 10s
        permitted-number-of-calls-in-half-open-state: 5

management:
  endpoints:
    web:
      exposure:
        include: health,info,circuitbreakers,prometheus
  metrics:
    tags:
      application: ${spring.application.name}

springdoc:
  swagger-ui:
    path: /swagger-ui.html
